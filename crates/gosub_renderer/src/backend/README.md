There are two rendering backends. One uses vello (graphical 2d render engine), and the other one is for rendering text mode.

Both backends implement the RenderBackend trait, which allows to do the rendering.